# 핏케어 루틴 보드 DESIGN.md

## 목적
이 문서는 `핏케어 루틴 보드`의 시각 언어와 인터페이스 규칙을 정의한다.  
이번 버전은 `linear.app`의 다크 제품 미학을 참고하되, 운동 보드라는 목적에 맞게 더 실용적이고 빠른 사용 흐름으로 재해석한다.

## 디자인 방향

### 1. 다크 모드가 기본이다
- 밝은 화면을 뒤집은 느낌이 아니라, 처음부터 어두운 표면 위에서 설계된 앱처럼 보여야 한다.
- 앱 배경은 거의 검정에 가까운 톤을 사용한다.
- 카드는 배경보다 아주 약간만 밝아야 하며, 덩어리감보다 계층감이 먼저 느껴져야 한다.

### 2. 정보가 먼저 보여야 한다
- 시선은 항상 `오늘 루틴 -> 현재 라운드 -> 다음 운동` 순으로 흐른다.
- 장식용 색, 장식용 그림자, 과한 그라디언트는 쓰지 않는다.
- 위계는 색보다 `타이포그래피, 간격, 밝기 차이`로 만든다.

### 3. 색은 아주 적게 쓴다
- 기본 팔레트는 거의 무채색에 가깝게 유지한다.
- 인디고/바이올렛은 활성 상태와 주요 CTA에만 사용한다.
- 초록은 완료, 성공, 진행 상태에만 제한적으로 사용한다.

### 4. 컴포넌트는 정밀하게 보인다
- 버튼, 카드, 입력창, 배지는 같은 시스템에서 나온 것처럼 보여야 한다.
- border는 선명한 회색이 아니라 `rgba(255,255,255,0.05) ~ 0.08` 범위의 얇은 라인을 사용한다.
- 그림자는 크고 퍼지는 대신, 얇은 ring과 낮은 대비의 그림자로 정리한다.

### 5. 모바일에서 즉시 이해되어야 한다
- 첫 화면은 "오늘 이걸 하면 된다"가 바로 보여야 한다.
- 현재 진행 중인 라운드만 펼쳐서 보여준다.
- 한 번에 너무 많은 운동 카드가 보이지 않도록 흐름을 접고 펼친다.

## 시각 톤
- 분위기: 차분함, 정밀함, 코치 앱 같은 신뢰감
- 참고 인상: Linear의 제품 화면처럼 조용하고 정확한 다크 UI
- 피해야 할 것:
  - 큰 반경의 장난감 같은 버튼
  - 카드마다 다른 배경색
  - 네온성 보라/파랑
  - 과한 유리 효과
  - 설명보다 장식이 먼저 보이는 화면

## 색상 시스템

### 배경
- 앱 배경: `#08090a`
- 기본 표면: `#111214`
- 보조 표면: `#17181b`
- hover 표면: `#1d1f23`

### 텍스트
- 기본 텍스트: `#f7f8f8`
- 보조 텍스트: `#b7bec8`
- 메타 텍스트: `#868b94`
- 약한 메타 텍스트: `#62666d`

### 포인트
- Primary: `#5e6ad2`
- Primary hover: `#7170ff`
- Success: `#27a644`
- Success soft: `rgba(39,166,68,0.14)`

### 보더
- 기본 보더: `rgba(255,255,255,0.08)`
- 약한 보더: `rgba(255,255,255,0.05)`
- 강조 보더: `rgba(94,106,210,0.28)`

## 타이포그래피
- 기본 폰트는 `Inter`를 사용한다.
- 헤드라인은 조금 더 조밀하게, 본문은 읽기 편한 line-height로 유지한다.
- 숫자, 진행 상태, 라벨은 본문보다 조금 더 단단하게 보이도록 한다.

### 위계
- 앱 메인 타이틀: 30~36px / 600 / 약한 음수 자간
- 섹션 타이틀: 20~24px / 600
- 카드 타이틀: 15~16px / 600
- 본문: 14~15px / 400~500
- 캡션/메타: 11~13px / 500

## 컴포넌트 규칙

### 버튼
- radius는 6px
- 기본 버튼은 아주 어두운 반투명 표면 + 얇은 보더
- 주요 버튼만 Primary 인디고 배경 사용
- hover는 밝기 변화만 주고, 형태가 크게 변하지 않게 한다

### 카드
- radius는 8px
- 카드 배경은 `rgba(255,255,255,0.03) ~ 0.05`
- 카드 내부에서 또 다른 카드 느낌이 과하게 생기지 않도록 표면 단계를 절제한다

### 입력 필드
- radius는 6px
- 배경은 카드보다 약간 눌린 표면
- 포커스는 보더와 약한 outer ring으로 표현한다

### 배지
- pill 형태는 유지하되, 높고 통통한 느낌보다 납작하고 정돈된 느낌을 우선한다
- 상태 배지는 크기보다 정보 밀도를 우선한다

## 레이아웃 원칙
- 최대 폭은 읽기 편한 모바일 대시보드 폭으로 제한한다
- 히어로는 큰 메시지보다 `오늘 상태 + 진행도 + 다음 액션`을 보여준다
- 본문은 기능별 섹션으로 나누되, 시각적으로 모두 같은 시스템 안에 있어야 한다
- 라운드 진행은 상단 요약, 현재 라운드는 본문 노출 방식으로 나눈다

## 현재 프로젝트에 바로 적용할 규칙
1. 배경 그라디언트를 줄이고 거의 단색에 가까운 dark canvas로 정리한다.
2. 모든 버튼 radius를 6px, 카드 radius를 8px 기준으로 통일한다.
3. 인디고는 CTA와 활성 상태에만 사용하고 나머지는 무채색 중심으로 유지한다.
4. 카드 보더는 얇고 미세하게, 그림자는 낮은 대비로 조정한다.
5. 히어로는 시각 효과보다 오늘 운동 정보의 밀도와 스캔 속도를 우선한다.
6. 운동 리스트는 현재 라운드만 집중해서 보이게 한다.

## 구현 메모
- 새 UI를 만들 때는 먼저 이 문서의 색상, 보더, 반경, 타이포 위계를 따른다.
- `linear.app/DESIGN.md`는 참고 원문으로 두고, 실제 프로젝트 적용 기준은 이 문서를 우선한다.
