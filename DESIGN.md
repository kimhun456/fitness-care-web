# 핏케어 루틴 보드 DESIGN.md

## 목적
이 문서는 `핏케어 루틴 보드`의 시각 언어와 인터페이스 규칙을 정의한다. 구현 규칙이 아니라 **어떻게 보여야 하는가**를 정리한 문서다.

이 문서는 Genie 위키의 다음 문서를 바탕으로 정리했다.
- `wiki/concepts/design-md.md`
- `wiki/sources/voltagent-awesome-design-md.md`
- `wiki/sources/claude-design-anthropic-labs.md`

## 핵심 원칙

### 1. 장식보다 계층
- 색을 많이 쓰지 않는다.
- 정보 우선순위는 **타이포그래피, 간격, 표면 밝기 차이**로 만든다.
- 한 화면에 시선이 가야 할 핵심은 1~2개만 둔다.

### 2. 다크 모드 네이티브
- 이 앱은 밝은 테마를 억지로 뒤집은 다크 모드가 아니라, 처음부터 다크 표면 위에 설계된 UI처럼 보여야 한다.
- 배경은 거의 검정에 가깝고, 카드와 패널은 한 단계 더 밝은 표면을 쓴다.
- 경계선은 선명한 색선보다 **반투명 화이트 라인**으로 처리한다.

### 3. 기능적 색 사용
- 포인트 컬러는 남발하지 않는다.
- 보라/인디고 계열은 현재 활성 상태와 주요 CTA에만 쓴다.
- 초록은 완료/성공/진행 같은 상태 표현에만 제한적으로 쓴다.
- 나머지는 회색 스케일 중심으로 유지한다.

### 4. 카드도 시스템으로 다룬다
- 카드마다 다른 느낌을 주지 않는다.
- 모든 카드가 같은 radius, 같은 border 강도, 같은 그림자 철학을 공유해야 한다.
- 깊이는 진한 그림자보다 **표면 밝기 단계와 미세한 ring**으로 만든다.

### 5. 모바일 우선
- 손가락으로 누르기 쉬운 버튼 높이를 기본으로 한다.
- 모바일에서는 한 번에 한 덩어리의 정보만 읽게 한다.
- 하단 퀵 액션 바, sticky 탭, 충분한 하단 safe area를 유지한다.

## 시각 톤
- 분위기: 차분함, 정돈됨, 코치 앱 같은 신뢰감
- 인상: 홈트/식단 앱이지만 피트니스 광고처럼 과장되지 않음
- 피해야 할 것:
  - 과한 그라디언트
  - 네온 느낌의 다색 포인트
  - 카드마다 다른 배경색
  - 둥글기만 큰 “장난감 같은” UI

## 색상 시스템

### 배경
- 앱 배경: 거의 검정에 가까운 차가운 다크 톤
- 기본 패널: 배경보다 한 단계 밝은 다크 그레이
- 강조 패널: 기본 패널보다 아주 약간 더 밝은 표면

### 텍스트
- 기본 텍스트: 순백이 아닌 부드러운 오프화이트
- 보조 텍스트: 차분한 회청색 계열의 muted gray
- 설명/메타: 더 낮은 대비의 tertiary text

### 포인트
- Primary accent: 인디고/바이올렛
- Success accent: 초록
- Danger: 리셋/삭제처럼 제한된 액션에만 절제해서 사용

## 타이포그래피
- 기본 폰트는 `Geist` 계열을 우선 사용한다.
- 헤드라인은 자간을 살짝 조여서 제품 UI 같은 밀도를 만든다.
- 본문은 읽기 편한 기본 자간과 line-height를 유지한다.
- 숫자, 상태, 작은 라벨은 조금 더 조밀하고 단단하게 보이도록 한다.

## 컴포넌트 규칙

### 버튼
- Primary: 짙은 인디고 배경, 밝은 텍스트
- Secondary: 초록 tint 또는 약한 surface tint
- Ghost: 배경 없이 ring만 있는 형태
- 모바일 터치 높이는 최소 44px 이상

### 카드
- 카드 배경은 반투명 dark surface
- border는 `rgba(255,255,255,0.08)` 수준의 미세한 라인
- 큰 그림자보다 ring + 약한 ambient shadow 사용

### 입력 필드
- field 배경은 카드보다 살짝 더 눌린 표면
- 포커스 시 accent ring이 보여야 한다
- placeholder는 텍스트보다 확실히 옅어야 한다

### 배지/필
- 정보 라벨은 pill 형태
- 색은 중립 회색 또는 accent tint만 사용
- 너무 크거나 도드라지지 않게 유지

## 레이아웃 규칙
- 최대 폭은 읽기 편한 dashboard 폭으로 제한한다.
- Hero는 큰 메시지 1개 + 상태 요약으로 단순하게 구성한다.
- 본문은 좌측 주 컨텐츠, 우측 보조 컨텐츠 구조를 유지하되 모바일에선 단일 컬럼으로 접는다.
- sticky 탭은 정보 구조를 바꾸는 UI이지 장식이 아니다. 항상 가독성을 우선한다.

## 이 프로젝트에 바로 적용할 지시
1. 전체 배경의 과한 그라디언트를 줄이고 더 절제된 dark surface로 바꾼다.
2. 카드, 버튼, 탭, 입력 필드의 radius/border/shadow를 시스템으로 통일한다.
3. 헤드라인과 라벨의 타이포 위계를 더 선명하게 만든다.
4. accent 색은 보라 중심으로 좁히고, 초록은 성공/물 섭취 등 일부 상태에만 쓴다.
5. 요약 카드와 메트릭 영역은 장식보다 정보 스캔 속도를 우선한다.
6. 모바일 하단 퀵바는 유지하되 시각적으로 더 정제된 시스템 안에 넣는다.

## 구현 메모
- `AGENTS.md`가 구현 절차를 정의한다면, 이 문서는 시각 규칙을 정의한다.
- 새 화면이나 새 컴포넌트를 추가할 때도 이 문서의 색상/타입/표면 규칙을 먼저 따른다.
